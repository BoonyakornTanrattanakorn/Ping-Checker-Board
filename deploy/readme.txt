- For depth first search please run DFS.bat
- The DFS algorithm use a heuristic that all pieces should move towards the center of the board (pieces on the left side should move to the right, vice versa).
- An interactive simulation of the board is also available. run GUI.bat

VM Argument:
java --module-path "lib\javafx-sdk-23\lib" --add-modules javafx.controls,javafx.graphics,javafx.fxml,javafx.media -jar GUI.jar

Code: Boonyakorn Tanrattanakorn